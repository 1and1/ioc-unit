<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>net.oneandone.ioc-unit</groupId>
    <artifactId>ioc-unit-parent</artifactId>
    <version>5.0.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>net.oneandone.ioc-unit:ioc-unit-parent</name>
    <description>The parent-pom for ioc-unit a module developed to ease test driven ejb3.x service-development.
    </description>
    <url>https://github.com/1and1/ioc-unit</url>

    <licenses>
        <license>
            <name>The Apache License, Version 2.0</name>
            <url>http://www.apache.org/licenses/LICENSE-2.0.txt</url>
        </license>
    </licenses>

    <developers>
        <developer>
            <name>Andreas Schörk</name>
            <email>andreas.schoerk@1and1.com</email>
            <organization>1and1</organization>
            <organizationUrl>http://www.1and1.com</organizationUrl>
        </developer>
    </developers>


    <properties>
        <!-- WildFly platform alignment: Jakarta specs, Hibernate, RESTEasy, Narayana etc.
             are managed by the imported WildFly BOMs below and must not be re-pinned here. -->
        <wildfly.version>39.0.1.Final</wildfly.version>
        <!--maven.javadoc.skip>true</maven.javadoc.skip-->
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <!-- apache plugin versions and configurations, please sort alphabetically -->
        <maven-checkstyle-plugin.version>3.6.0</maven-checkstyle-plugin.version>
        <maven-checkstyle-plugin.configLocation>site/checkstyle.xml</maven-checkstyle-plugin.configLocation>
        <maven-checkstyle-plugin.suppressionsLocation>checkstyle_suppressions.xml</maven-checkstyle-plugin.suppressionsLocation>
        <maven-dependency-plugin.version>3.11.0</maven-dependency-plugin.version>
        <maven-deploy-plugin.version>3.1.4</maven-deploy-plugin.version>
        <maven-compiler-plugin.version>3.15.0</maven-compiler-plugin.version>
        <!-- WildFly 39 requires Java 17+ at runtime; align the build toolchain accordingly. -->
        <maven.compiler.release>17</maven.compiler.release>
        <maven-gpg-plugin.version>3.2.8</maven-gpg-plugin.version>
        <central-publishing-maven-plugin.version>0.11.0</central-publishing-maven-plugin.version>
        <maven-javadoc-plugin.version>3.12.0</maven-javadoc-plugin.version>

        <maven-scm-plugin.version>2.2.1</maven-scm-plugin.version>
        <maven-scm-publish-plugin.version>3.3.0</maven-scm-publish-plugin.version>
        <maven-release-plugin.version>3.3.1</maven-release-plugin.version>
        <maven-resources-plugin.version>3.5.0</maven-resources-plugin.version>
        <maven-site-plugin.version>3.22.0</maven-site-plugin.version>
        <maven-source-plugin.version>3.4.0</maven-source-plugin.version>
        <maven-surefire-plugin.version>3.5.6</maven-surefire-plugin.version>
        <maven-war-plugin.version>3.5.1</maven-war-plugin.version>

        <!-- external -->
        <logback.version>1.5.38</logback.version>

        <!-- test -->
        <mockito.version>4.11.0</mockito.version>
        <junit4.version>4.13.2</junit4.version>
        <junit5.version>5.14.4</junit5.version>
        <mockrunner.version>2.0.4</mockrunner.version>
        <version.cglib>3.2.5</version.cglib>

        <deployment.port>10099</deployment.port>
        <deployment.targetdir>${basedir}/target/</deployment.targetdir>
        <mysql.connector.version>8.0.33</mysql.connector.version>
        <h2.version>2.4.240</h2.version>
        <hamcrest.version>2.1</hamcrest.version>

        <site.deploy.base.url>http://maven-sites.server.lan</site.deploy.base.url>
        <site-url>${site.deploy.base.url}/${project.groupId}/${project.version}</site-url>
        <javassist.version>3.27.0-GA</javassist.version>

        <reflections.version>0.9.12</reflections.version>
        <jaxrpc.version>1.1</jaxrpc.version>

        <camunda-engine.version>7.5.0</camunda-engine.version>
        <additionalparam>-Xdoclint:none</additionalparam>

        <!-- Weld is not part of the WildFly BOM (container-internal); keep pinned.
             weld-spi and weld-api are always bundled by WildFly at the same version as each
             other (confirmed for WildFly 39.0.1.Final: both weld-spi-5.0.SP3.jar and
             weld-api-5.0.SP3.jar), so one property drives both "provided"-scope
             dependencyManagement entries below (used by ioc-unit/ioc-unit-contexts/
             ioc-unit-resteasy for real-container use). -->
        <weld-spi-api.version>5.0.SP3</weld-spi-api.version>
        <!-- jakarta.el:jakarta.el-api is NOT managed under this GA by the WildFly BOM
             (WildFly manages org.jboss.spec.jakarta.el:jboss-el-api_5.0_spec instead),
             so it stays explicitly pinned where used. -->

        <surefire-argline>
            -agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=5006
        </surefire-argline>
    </properties>
    <profiles>
        <profile>
            <id>release-sign-artifacts</id>
            <activation>
                <property>
                    <name>performRelease</name>
                    <value>true</value>
                </property>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-gpg-plugin</artifactId>
                        <version>${maven-gpg-plugin.version}</version>
                        <executions>
                            <execution>
                                <id>sign-artifacts</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>sign</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                    <!-- Publishes release artifacts to Maven Central via the Central Portal
                         Publisher API. Only active when performRelease=true (e.g. during
                         mvn release:perform), so plain "mvn deploy" (SNAPSHOTs) is unaffected
                         and continues to go to the internal Artifactory snapshot repository. -->
                    <plugin>
                        <groupId>org.sonatype.central</groupId>
                        <artifactId>central-publishing-maven-plugin</artifactId>
                        <version>${central-publishing-maven-plugin.version}</version>
                        <extensions>true</extensions>
                        <configuration>
                            <publishingServerId>central</publishingServerId>
                            <autoPublish>true</autoPublish>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
    <modules>
        <module>boms/ioc-unit-bom</module>
        <module>weld-starter</module>
        <module>modules/ioc-unit-analyzer</module>
        <module>modules/ioc-unit</module>
        <module>modules/ioc-unit-mockseasy</module>
        <module>modules/ioc-unit-resource</module>
        <module>modules/ioc-unit-jta-jpa</module>
        <module>modules/ioc-unit-validate</module>
        <module>modules/ioc-unit-resteasy</module>
        <module>modules/ioc-unit-jms</module>
        <module>modules/ioc-unit-ejb</module>
        <module>modules/ioc-unit-dep-impls</module>
        <module>modules/ioc-unit-contexts</module>
        <module>testing</module>
    </modules>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.wildfly.bom</groupId>
                <artifactId>wildfly-ee-with-tools</artifactId>
                <version>${wildfly.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.wildfly.bom</groupId>
                <artifactId>wildfly-expansion-with-tools</artifactId>
                <version>${wildfly.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- Internal modules -->
            <dependency>
                <groupId>net.oneandone.ioc-unit</groupId>
                <artifactId>weld-starter-base</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>net.oneandone.ioc-unit</groupId>
                <artifactId>weld-starter</artifactId>
                <version>${project.version}</version>
                <scope>test</scope>
            </dependency>

            <!-- Mockito is not part of the WildFly BOM (pure test/mocking library); pin it once here
                 so every module uses the same version instead of each declaring its own. -->
            <dependency>
                <groupId>org.mockito</groupId>
                <artifactId>mockito-core</artifactId>
                <version>${mockito.version}</version>
            </dependency>

            <!-- testing -->
            <dependency>
                <groupId>org.junit.jupiter</groupId>
                <artifactId>junit-jupiter-params</artifactId>
                <version>${junit5.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>org.junit.jupiter</groupId>
                <artifactId>junit-jupiter-api</artifactId>
                <version>${junit5.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>org.junit.jupiter</groupId>
                <artifactId>junit-jupiter-engine</artifactId>
                <version>${junit5.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>org.hamcrest</groupId>
                <artifactId>hamcrest</artifactId>
                <version>${hamcrest.version}</version>
                <scope>test</scope>
            </dependency>

            <!-- Jakarta specs (jakarta.enterprise.cdi-api, jakarta.annotation-api, jakarta.validation-api,
                 jakarta.interceptor-api, jakarta.ejb-api, jakarta.transaction-api, jakarta.inject-api, etc.)
                 are intentionally NOT listed here: their versions are supplied transitively by the imported
                 WildFly BOMs above. Each module declares the specs it actually uses (scope "provided",
                 no <version>) and picks up the version from the BOM import. -->
            <!-- Weld is not part of the WildFly BOM (container-internal); keep pinned. -->
            <dependency>
                <groupId>org.jboss.weld</groupId>
                <artifactId>weld-spi</artifactId>
                <version>${weld-spi-api.version}</version>
                <scope>provided</scope>
            </dependency>
            
            <!-- logging -->
            <dependency>
                <groupId>ch.qos.logback</groupId>
                <artifactId>logback-classic</artifactId>
                <version>${logback.version}</version>
                <scope>test</scope>
            </dependency>

            <!-- testing -->
            <dependency>
                <groupId>org.junit.vintage</groupId>
                <artifactId>junit-vintage-engine</artifactId>
                <version>${junit5.version}</version>
                <scope>test</scope>
            </dependency>
            <dependency>
                <groupId>junit</groupId>
                <artifactId>junit</artifactId>
                <version>${junit4.version}</version>
                <scope>provided</scope>
            </dependency>
            <dependency>
                <groupId>org.junit</groupId>
                <artifactId>junit-bom</artifactId>
                <version>${junit5.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.javassist</groupId>
                <artifactId>javassist</artifactId>
                <version>${javassist.version}</version>
                <scope>test</scope>
            </dependency>

            <dependency>
                <groupId>org.reflections</groupId>
                <artifactId>reflections</artifactId>
                <version>${reflections.version}</version>
            </dependency>
            <dependency>
                <groupId>com.rabbitmq</groupId>
                <artifactId>amqp-client</artifactId>
                <version>5.34.0</version>
            </dependency>
            <dependency>
                <groupId>commons-logging</groupId>
                <artifactId>commons-logging</artifactId>
                <version>1.3.6</version>
            </dependency>
        </dependencies>
    </dependencyManagement>
    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-enforcer-plugin</artifactId>
                <version>3.6.3</version>
                <executions>
                    <execution>
                        <id>enforce-build-environment</id>
                        <goals>
                            <goal>enforce</goal>
                        </goals>
                        <configuration>
                            <rules>
                                <requireJavaVersion>
                                    <version>[17,)</version>
                                </requireJavaVersion>
                                <requireMavenVersion>
                                    <version>[3.8.0,)</version>
                                </requireMavenVersion>
                                <!-- Fails the build on conflicting transitive versions instead of
                                     silently letting Maven's "nearest wins" pick one - this is what
                                     protects consumers from the double-BOM leakage this migration
                                     fixes from recurring silently in the future. -->
                                <dependencyConvergence/>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${maven-compiler-plugin.version}</version>
                <configuration>
                    <release>${maven.compiler.release}</release>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <version>${maven-dependency-plugin.version}</version>
                <executions>
                    <execution>
                        <id>analyze-only</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>analyze-only</goal>
                        </goals>
                        <configuration>
                            <!-- Flags declared-but-unused and used-but-undeclared dependencies -
                                 the classic source of accidental transitive leakage. Reports only
                                 for now (failOnWarning=false) since the reactor hasn't been fully
                                 swept yet; tighten to true once clean. -->
                            <failOnWarning>false</failOnWarning>
                            <ignoreNonCompile>true</ignoreNonCompile>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-checkstyle-plugin</artifactId>
                <version>${maven-checkstyle-plugin.version}</version>
                <configuration>
                    <configLocation>site/checkstyle.xml</configLocation>
                    <suppressionsLocation>site/checkstyle_suppressions.xml</suppressionsLocation>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>${maven-surefire-plugin.version}</version>
                <configuration>
                    <redirectTestOutputToFile>true</redirectTestOutputToFile>
                    <!-- docker test container bug: https://github.com/testcontainers/testcontainers-java/issues/11212 -->
                    <systemPropertyVariables>
                        <api.version>1.44</api.version>
                    </systemPropertyVariables>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-scm-plugin</artifactId>
                <version>${maven-scm-plugin.version}</version>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-scm-publish-plugin</artifactId>
                <version>${maven-scm-publish-plugin.version}</version>
                <inherited>true</inherited>
                <configuration>
                    <pubScmUrl>${project.scm.developerConnection}</pubScmUrl>
                    <scmBranch>gh-pages</scmBranch>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>
                <executions>
                    <execution>
                        <id>attach-javadocs</id>
                        <goals>
                            <goal>jar</goal>
                        </goals>
                        <configuration>
                            <doclint>none</doclint>
                            <source>${maven.compiler.release}</source>
                            <additionalOptions>-Xdoclint:none</additionalOptions>
                        </configuration>
                    </execution>
                </executions>
                <configuration>
                    <detectOfflineLinks>false</detectOfflineLinks>
                    <failOnError>false</failOnError>
                    <doclint>none</doclint>
                    <additionalOptions>-Xdoclint:none</additionalOptions>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
                <executions>
                    <execution>
                        <goals>
                            <goal>jar-no-fork</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.basepom.maven</groupId>
                <artifactId>duplicate-finder-maven-plugin</artifactId>
                <version>2.0.1</version>
                <executions>
                    <execution>
                        <id>duplicate-check</id>
                        <phase>package</phase>
                        <goals>
                            <goal>check</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <failBuildInCaseOfDifferentContentConflict>false</failBuildInCaseOfDifferentContentConflict>
                    <failBuildInCaseOfEqualContentConflict>false</failBuildInCaseOfEqualContentConflict>
                    <failBuildInCaseOfConflict>false</failBuildInCaseOfConflict>
                    <checkCompileClasspath>true</checkCompileClasspath>
                    <checkRuntimeClasspath>true</checkRuntimeClasspath>
                    <checkTestClasspath>false</checkTestClasspath>
                    <ignoredResourcePatterns>
                        <ignoredResourcePattern>.*\.gitkeep$</ignoredResourcePattern>
                        <ignoredResourcePattern>log4j\.properties</ignoredResourcePattern>
                        <!-- Jetty about.html pages -->
                        <ignoredResourcePattern>.*about\.html$</ignoredResourcePattern>
                        <ignoredResourcePattern>readme.html</ignoredResourcePattern>
                        <ignoredResourcePattern>license.html</ignoredResourcePattern>
                        <!--
                            Found duplicate (but equal) resources in
                            [com.github.fge:json-schema-core:1.2.5, com.github.fge:json-schema-validator:2.2.6]
                        -->
                        <ignoredResourcePattern>.*draftv[34]/schema$</ignoredResourcePattern>
                        <!-- Scala lang -->
                        <ignoredResourcePattern>rootdoc\.txt</ignoredResourcePattern>
                    </ignoredResourcePatterns>
                    <ignoredClassPatterns>
                        <!-- Jigsaw project (Java 9) and later module-info classes. -->
                        <ignoredClassPattern>.*\.module-info$</ignoredClassPattern>
                        <ignoredClassPattern>javax\.activation.*</ignoredClassPattern>
                        <!-- Spring AOP -->
                        <ignoredClassPattern>org\.aopalliance.*</ignoredClassPattern>
                        <!-- OSGi -->
                        <ignoredClassPattern>org\.osgi.*</ignoredClassPattern>
                        <ignoredClassPattern>org\.ops4j.*</ignoredClassPattern>
                    </ignoredClassPatterns>
                </configuration>
            </plugin>

        </plugins>

        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-deploy-plugin</artifactId>
                    <version>${maven-deploy-plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-source-plugin</artifactId>
                    <version>${maven-source-plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-site-plugin</artifactId>
                    <version>${maven-site-plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-javadoc-plugin</artifactId>
                    <version>${maven-javadoc-plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-release-plugin</artifactId>
                    <version>${maven-release-plugin.version}</version>
                    <inherited>true</inherited>
                    <configuration>
                        <!-- Keep tags consistent with the project's existing "vX.Y.Z" convention
                             instead of the plugin default "artifactId-version". -->
                        <tagNameFormat>v@{project.version}</tagNameFormat>
                    </configuration>
                    <!-- No explicit maven-scm-provider-gitexe dependency override here:
                         maven-release-plugin 3.3.1 already brings in a consistent, matching
                         set of maven-scm-* 2.2.1 artifacts transitively. Pinning an old
                         provider version (e.g. 1.9.2) mixes incompatible SCM API classes and
                         causes NoSuchMethodError at runtime (AbstractGitScmProvider.getLogger()). -->
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

    <scm>
        <url>https://github.com/1and1/ioc-unit</url>
        <connection>scm:git:https://github.com/1and1/ioc-unit.git</connection>
        <developerConnection>scm:git:ssh://git@github.com/1and1/ioc-unit.git</developerConnection>
        <tag>HEAD</tag>
    </scm>
    <issueManagement>
        <system>GitHub</system>
        <url>https://github.com/1and1/ioc-unit/issues/</url>
    </issueManagement>
    <inceptionYear>2017</inceptionYear>

    <distributionManagement>
        <!-- Releases go to Maven Central via the central-publishing-maven-plugin
             (see release-sign-artifacts profile); no repository entry is needed
             here for that, the plugin talks to the Central Portal API directly. -->
        <snapshotRepository>
            <uniqueVersion>true</uniqueVersion>
            <id>artifactory-snapshots</id>
            <name>access-snapshot</name>
            <url>https://artifactory.1and1.org/artifactory/access-snapshot</url>
        </snapshotRepository>
    </distributionManagement>

</project>
